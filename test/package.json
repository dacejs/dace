{
  "scripts": {
    "start": "dace",
    "start:dist": "NODE_ENV=production node prd/server.js",
    "build": "NODE_ENV=local dace build",
    "test": "npm run build && npm run start:dist"
  },
  "dependencies": {
    "dace": ".."
  }
}
