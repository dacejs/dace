/* eslint-disable */
import React from 'react';

export default () => {
  const exampleName = do {
    if (false) {
      'xx';
    } else {
      'with-babelrc';
    }
  };

  return (
    <div>
      <h1>{exampleName}</h1>
    </div>
  );
};
